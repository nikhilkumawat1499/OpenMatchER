# Designing OpenMatchER

OpenMatchER is designed as a modular entity-resolution platform: API, UI, matching engine, LLM providers, execution engines, and benchmarks are separate components.

That separation supports local development today and distributed execution tomorrow.

