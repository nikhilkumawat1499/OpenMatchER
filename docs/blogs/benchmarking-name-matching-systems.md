# Benchmarking Name Matching Systems

Name matching should be evaluated with precision, recall, F1, false positives, false negatives, runtime, and memory. A model that looks strong on exact duplicates can fail on aliases, abbreviations, and versioned product names.

OpenMatchER includes demo datasets and a benchmark runner to make tradeoffs visible.

